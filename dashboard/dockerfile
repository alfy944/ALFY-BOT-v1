# Usa immagine Python leggera
FROM python:3.10-slim

# Imposta cartella di lavoro
WORKDIR /app

# Copia i requisiti e installa
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copia tutto il codice (app.py e cartella templates)
COPY . .

# Espone la porta
EXPOSE 8501

# Comando corretto per FastAPI
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8501"]
